apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mykadoo.fullname" . }}-config
  labels:
    {{- include "mykadoo.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
